# TGSimilarSpam

Автоматизированный бот для рекурсивного поиска похожих Telegram-каналов и отправки сообщений их владельцам (BD рассылка).

**Ключевые возможности:**
- 🔍 Рекурсивный поиск каналов (L0→L1→L2 граф рекомендаций)
- 🌍 Двухуровневое определение языка (описание + посты)
- 📧 Автоматическая отправка сообщений владельцам каналов
- 🛡️ Защита от спама: cooldown 2 дня, фильтр ботов, проверка истории
- 🗂️ Логирование в Excel + JSON (sent_log.json)
- 🇷🇺 🇬🇧 Поддержка русского и английского языков
- ⚙️ Интерактивный setup с авторизацией в Telegram

## Быстрый старт

### 1. Клонируй репозиторий
```bash
git clone https://github.com/moxquattuor/TGSimilarSpam.git
cd TGSimilarSpam
```

### 2. Установи зависимости
```bash
pip install -r requirements.txt
```

### 3. Запусти интерактивный setup
```bash
python setup.py
```

Setup запросит:
- Номер телефона Telegram (авторизация)
- Ключевые слова для поиска
- Целевой язык каналов (RU/EN/BOTH)
- Seed-каналы (стартовые точки для поиска)
- Текст сообщения для RU и EN

### 4. Запусти бота
```bash
python main_bot.py
```

## Конфигурация

После `setup.py` создаётся `config.json`:

```json
{
  "interface_lang": "ru",
  "phone": "+1234567890",
  "api_id": 20190360,
  "api_hash": "your_api_hash_here",
  "seed_channels": ["CryptoPlanetCalls", "Whales_Pumps"],
  "target_language": "RU",
  "msg_ru": "Ваш текст...",
  "msg_en": "Your text...",
  "cooldown_days": 2,
  "send_delay_seconds": 15,
  "max_sent_per_run": 50
}
```

Подробнее см. [CONFIG.md](docs/CONFIG.md)

## Как это работает

1. **Поиск каналов** — начинает с seed-каналов, затем рекурсивно ищет похожие через `GetChannelRecommendationsRequest`
2. **Анализ языка** — проверяет описание канала (about), затем анализирует последние посты
3. **Поиск владельцев** — извлекает юзернеймы из описания канала (@username)
4. **Отправка сообщений** — проверяет историю, cooldown, фильтр ботов, затем отправляет
5. **Логирование** — сохраняет результаты в Excel и JSON

## Защита от проблем

- ✅ **Cooldown** — не пишет одному контакту чаще чем раз в N дней
- ✅ **История** — не отправляет если уже был ответ на старое сообщение
- ✅ **Фильтр ботов** — пропускает аккаунты заканчивающиеся на "bot"
- ✅ **Удаление старых** — если были старые неответные сообщения, удаляет перед новым
- ✅ **Интервалы** — соблюдает задержку между отправками (15 сек по умолчанию)

## Требования

- Python 3.8+
- Telegram аккаунт
- ~200-500 MB свободного места

## API Credentials (опционально)

Бот работает с **дефолтными публичными Telethon credentials** и не требует своих API key.

Если хочешь использовать свои credentials (для лучшего rate limit на масштабе):

1. Перейди на https://my.telegram.org/apps
2. Авторизуйся через телефон
3. Создай приложение (App name любое)
4. Скопируй `API ID` и `API Hash`
5. Передай в setup.py или установи environment переменные:
   ```bash
   export TELEGRAM_API_ID=your_id
   export TELEGRAM_API_HASH=your_hash
   ```

## Файлы проекта

**Исходные:**
- `setup.py` — интерактивная конфигурация
- `main_bot.py` — основной бот

**Генерируются автоматически:**
- `config.json` — твои настройки (удали перед выгрузкой на GitHub)
- `sent_log.json` — лог отправок для cooldown (удали перед выгрузкой)
- `contacts.xlsx` — **РЕЗУЛЬТАТЫ**: все обработанные каналы и контакты
- `session_PHONE.session` — сессия Telegram (удали перед выгрузкой)

## Примеры

### Отправить 10 сообщений
```json
{
  "max_sent_per_run": 10
}
```

### Только русские каналы
```json
{
  "target_language": "RU"
}
```

### Увеличить интервал между сообщениями
```json
{
  "send_delay_seconds": 30
}
```

## Результаты Работы

После запуска bot создаёт файл **`contacts.xlsx`** с результатами:

| Channel Link | Owner Username |
|---|---|
| https://t.me/channel1 | @owner1 |
| https://t.me/channel2 | @owner2 |
| https://t.me/channel3 | NOT_FOUND |

**2 столбца:**
- **Column 1**: Ссылка на канал
- **Column 2**: Юзернейм владельца (или "NOT_FOUND" если владелец не найден)

**Важно:**
- ✅ Новые строки **добавляются**, не перезаписываются
- ✅ Если запустить бота 3 раза = в файле будут результаты всех 3 запусков
- ✅ Один файл на все запуски (не куча файлов)
- ✅ Можно легко импортировать в CRM или обработать дальше

## Документация

- [SETUP_GUIDE.md](docs/SETUP_GUIDE.md) — пошаговая инструкция
- [CONFIG.md](docs/CONFIG.md) — все параметры конфига
- [FEATURES.md](docs/FEATURES.md) — детальное описание возможностей

## Проблемы и решения

**Вопрос:** Бот не находит владельцев
**Ответ:** Убедись что seed-каналы популярные (>1000 подписчиков) и имеют контакты в описании

**Вопрос:** "database is locked" ошибка
**Ответ:** Остановись старые процессы Python, очисти сессию: `rm session_*.session`

**Вопрос:** Как получить больше каналов?
**Ответ:** Увеличь `max_sent_per_run` в config.json или добавь больше seed-каналов

## Лицензия

MIT — используй как хочешь

## Дисклеймер

⚠️ Используй бота ответственно:
- Соблюдай правила Telegram
- Не спами, не отправляй пользователям без согласия
- Используй для легитимного бизнеса (партнёрства, реклама и т.д.)
- Автор не несёт ответственность за неправомерное использование

---

**Вопросы?** Создай Issue на GitHub или свяжись через email.

**Нравится?** ⭐ Star репо!
